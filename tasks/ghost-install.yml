---
- name: Make sure install dir exsists
  file:
    path: "{{ item['dir'] }}"
    state: directory


- name: Install Ghost
  command:
    chdir: "{{ item.dir }}"
    cmd: "ghost install {{ item['type'] | default(omit) }} --no-setup"
